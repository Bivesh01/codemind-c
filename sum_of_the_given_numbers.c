#include<stdio.h>
int main()
{
    int n,i,arr1[i],arr2[i],sum;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr1[i]);
        scanf("%d",&arr2[i]);
        sum=arr1[i]+arr2[i];
        printf("%d
",sum);
    }
}